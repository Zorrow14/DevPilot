// Express app setup will be completed after dependencies are installed.
// Planned setup:
// - create an Express app
// - configure CORS and JSON parsing
// - mount routes from routes/index.routes.ts
// - register not-found and error middleware

export const appPlaceholder = {
  name: "DevPilot API",
  status: "Express setup pending",
};
